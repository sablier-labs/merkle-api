export PINATA_ACCESS_TOKEN=
export PINATA_API_KEY=
export PINATA_API_SERVER=
export PINATA_SECRET_API_KEY=
export IPFS_GATEWAY=
export MERKLE_API_BEARER_TOKEN=

# Upstash Redis REST API. Optional: if unset, CID caching and rate limiting are disabled (fail-open).
export UPSTASH_REDIS_REST_URL=
export UPSTASH_REDIS_REST_TOKEN=